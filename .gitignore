# dependencies
node_modules/

# build
dist/
dist-ssr/
*.local

# env (절대 커밋 금지 — API 키 포함)
.env

# 궤적 원본 데이터 (용량이 크고 주행마다 달라진다 — 저장소에 넣지 않는다)
*.csv
!docs/**/*.csv

# editor
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
