# 궤적 비교 (traj-compare)

사전 매핑 궤적 CSV와 실제 주행 궤적 CSV를 카카오 위성지도 위에 겹쳐 보는 웹 앱.

## 스택
- Vite + React + TypeScript (백엔드 없음, 의존성 최소)
- 지도: 카카오맵 JS SDK — JavaScript 키는 `.env`의 `VITE_KAKAO_JS_KEY` (커밋 금지)
- 위성: `kakao.maps.MapTypeId.SKYVIEW` / 일반: `ROADMAP`. **위성+도로(`HYBRID`)는 뺐다**

## 원칙
- **CSV는 브라우저 밖으로 나가지 않는다.** 업로드·전송·저장 기능을 제안하지 말 것
- 파일 유형은 **사용자에게 묻지 않고 열 이름으로 판별**한다 (`src/lib/csv.ts`)
  - `latitude`/`longitude` → 매핑, `fix_lat`/`fix_lon` → 주행
- 거리 계산은 **국소평면 근사**로 통일한다 (`x = R·Δlon·cos(lat0)`, `y = R·Δlat`).
  원점은 매핑 궤적의 첫 점 — 모든 궤적이 같은 원점을 써야 겹쳐진다
- 주행 기록 CSV의 **hold 중복(연속 동일 좌표)은 합친다**. 안 그러면 오차 통계가 왜곡된다
  - 단 **제어 신호(20Hz)는 합치지 않는다** — GPS는 5Hz라 같은 규칙을 적용하면 네 표본 중
    셋이 사라진다. `ParsedTrack.control`이 병합 전 전 행을 따로 들고 있다 (`src/lib/csv.ts`)
- CSV 파싱은 **따옴표를 지켜야** 한다 (`board_status` 열에 쉼표가 들어 있다)
- 위성 영상 위 UI라 어두운 반투명 + 흰 글씨, 선에는 어두운 테두리를 깐다
- GPS 정밀도는 **σ 기준 3등급**(≤5cm / ≤30cm / 그 외)으로 나눈다. 이진 판정으로는
  파일 간 차이(σ 1.1cm↔2.2m 대 5~15cm)가 드러나지 않는다
- 판정 열은 `gps_sigma_m` → `fix_cov_xx` 순, `gps_quality`는 **4일 때만** 등급을 올린다.
  `fix_status`는 σ가 없을 때만, 값이 두 종류 이상일 때만 쓴다
- **무엇으로 판정했는지와 상태값 원본을 항상 화면에 표시**한다. 근거를 감추지 말 것
- 정밀할수록 밝게 — 지도와 그래프 띠가 **같은 규칙**을 쓴다
- **매핑 CSV 없이 주행만 올려도 동작해야 한다.** 그때는 그래프 세로축이 σ로 바뀐다
- 열이 없으면 **그 그래프만 빠진다.** 옛 기록(2026-08-12 이전)에는 `gps_*`·`cb_state`·
  `brake_pot`이 아예 없다 — 없는 것을 0으로 채우지 말 것
- **그릴 세로축이 없어도 막대는 남긴다.** 매핑 기준도 GPS 정밀도 열도 없으면
  `series.kind = "none"`(`values: null`)으로 곡선 없이 가로축(주행거리)만 그린다.
  막대까지 없애면 **지점을 고를 길이 사라져 위경도를 못 읽는다** — 값이 없다는 것을
  0으로 그려 속이지 않으면서 좌표 확인은 되게 하는 절충이다 (`ErrorProfile`의 `.lane`)

## 제어 진단 (`src/lib/control.ts`, `src/components/ControlPanel.tsx`)
`/cmd_vel_raw`와 `/drive_diag` 열로 순수추종·CTE 적분이 실제로 돌았는지 본다.
- `driving.py`의 조향 계산을 **다시 돌려 `cmd_steer_deg`와 비교**한다(재현 잔차).
  쓰는 상수는 `driving.py`의 복제본이다 — **단일 소유자는 그쪽**이고, 차량 코드가 바뀌면
  이 검산이 조용히 틀린다. 그래서 상수를 화면에 그대로 띄운다
- `heading_err_deg`는 **헤딩 오차가 아니다.** 순수추종 목표점의 차체기준 방위 α다
  (`driving.py:2836` — 예전엔 인덱스 WP 방위오차였다). 라벨을 "헤딩 오차"로 달지 말 것
- `cmd_steer_deg`/`steer_measured_deg`는 **pot 지령각**이지 도로휠각이 아니다
  (도로휠각 ≈ pot ÷ `STEER_PLANT_GAIN` 1.26)
- 기준선(±40° 상한, ±2 m 이탈 정지)은 **눈금을 넓히지 않는다.** 넣으면 실제 신호가
  한 줄로 눌린다 — 실측에서 CTE −0.70~0.29 m가 ±2.16 눈금에 깔려 직선으로 보였다
- 속도는 **`meas_pulse` × 0.884**를 쓴다. `encoder_sum`은 아래 "검증된 것" 참고

## 궤적 생성 (`src/lib/route.ts`, `src/components/DrawPanel.tsx`)
지도를 클릭해 매핑 CSV를 손으로 만든다. 카카오 '거리 재기'와 같은 손버릇이다.
- 클릭 지점은 **직전 점에서 간격의 정수배** 자리로 당겨 붙고 그 사이는 자동 보간된다.
  방향은 클릭한 그대로 두고 **거리만** 반올림한다 — 방향까지 물리면 대각선을 못 긋는다
- **보간은 직선뿐.** 곡선 후처리는 하지 않는다
- 커서와 '지금 클릭하면 찍힐 자리'를 **둘 다** 표시한다. 벌어진 만큼이 격자로 당겨진 거리다
- 좌클릭 점 찍기 / 끌기 지도 이동(5px 넘으면 끌기) / Backspace 직전 **구간** 되돌리기 /
  ESC·우클릭 지정 완료. 한 번에 하나만 그린다
- **'보여 주기'와 '입력 받기'를 갈라 둔다** (`DrawState.active`). 매핑을 종료해도 그린
  궤적은 지도에 남아야 해서 `DrawState` 객체는 계속 넘어간다 — 객체 유무로 입력 허용을
  판단하면 **종료 뒤에도 클릭이 먹는다**. 실제로 그렇게 냈다가 잡았다(81점 → 194점)
- 내려받는 파일명은 **`route_`로 시작해야 한다** — `prompt.py`가 그 접두어로만 거른다
- 실계측 열(`wheel_pulse` 등)은 **빈칸으로 둔다.** 0을 적으면 '측정했더니 0'으로 읽힌다

## 서비스워커 — `public/sw.js`는 청소 전용이며 **지우면 안 된다**
이 앱은 서비스워커를 쓰지 않는다. 그런데 화장실SOS 시절 `vite-plugin-pwa`가 등록한
워커가 방문자 브라우저에 남아 **옛 화면을 캐시에서 돌려주는** 문제가 있었다
(첫 로드에 화장실 지도, `Ctrl+Shift+R`을 해야 이 앱이 뜸). 스크립트가 404가 되어도
**서비스워커 등록은 해제되지 않는다** — 그래서 같은 자리에 스스로 등록을 해제하고
캐시를 지우는 워커를 올려 둔 것이다. 근거와 동작은 파일 주석에 있다.
`src/main.tsx`에도 `getRegistrations().unregister()` 두 번째 그물이 있다.
**앞으로 PWA 기능을 다시 넣지 말 것** — 넣는 순간 이 문제가 되돌아온다.

## 안드로이드 매핑 앱에서 링크로 CSV 받기 (`src/lib/inbound.ts`)
휴대폰 간이 매핑 앱(`gpsmapper`, 별도 리포)이 CSV를 **URL 프래그먼트**에 실어 보낸다.
`#csvgz=<base64url(gzip(CSV))>&name=<파일명>` 형식이고, 앱에서 파일을 고르면 이 주소가
열려 곧바로 지도에 올라간다. 프래그먼트가 없으면 종전대로 빈 화면이다.
- **쿼리(`?`)로 바꾸지 말 것.** 프래그먼트는 서버로 전송되지 않아서 쓴 것이다 —
  쿼리로 보내면 GitHub Pages 접근 로그에 주행 좌표가 남고, 위의 "CSV는 브라우저 밖으로
  나가지 않는다" 원칙이 깨진다
- 꺼낸 즉시 `replaceState`로 프래그먼트를 지운다(중복 적재·주소 공유 시 좌표 유출 방지)
- 받은 CSV는 `File`로 감싸 **손으로 올린 파일과 같은 경로**(`addFiles`)를 타게 한다

## 명령어
- `npm run dev` — 개발 서버 (http://localhost:5173)
- `npm run build` — `tsc --noEmit` + vite build
- `npm run preview` — 빌드 결과 확인 (http://localhost:4173)

카카오 콘솔의 **플랫폼 > Web > 사이트 도메인**에 위 주소를 등록해야 지도가 뜬다.
등록이 빠지면 지도가 회색으로만 나온다.

## 검증된 것 (실측 데이터 기준)
- 매핑 383점 / 주행 3,222행 → 806점(hold 2,416행 합침). **제어 시계열은 3,222행 그대로**
- **`encoder_sum`을 속도로 쓰지 말 것** (2026-08-12 기록, GPS 실주행 163.6 m / 161 s 기준
  적분): `meas_pulse`×0.884 → **164.0 m (1.00배)** / `encoder_sum`÷2×0.884 → 1542.3 m
  (**9.43배**) / `cmd_pulse`×0.884 → 383.8 m (2.35배, 지령이라 정상).
  즉 그 빌드의 `/encoder`는 `ENC_SUM_TO_PULSE`×`MS_PER_PULSE`가 가정한 계측창(20 ms)이
  아니다. 31.8%의 행이 물리 상한을 넘고 **1초짜리 덩어리**라 중앙값 필터로도 안 지워진다
  (3·5·7·9점 모두 그대로). **미확인**: 그 계측창이 무엇인지
- 조향 지령 재현은 `driving.py`와 **1.4e-14°** 까지 일치한다(무작위 400표본 대조).
  그런데 **2026-08-12 실측 기록에서는 잔차 RMS 2.14° / 최대 15.8°** 로 벌어진다.
  원인 미확인 — 아래를 다 배제했다: 링키지비 1.26/1.75 조합(1.26이 그나마 낫다),
  토픽 간 샘플 어긋남(±3틱 전부 개선 없음). 최소자승으로 뽑으면 g=1.382·u=0.54 로
  현재값(1.26·5.17)도 옛값(1.75·7.2)도 아니다. **차량 쪽에서 확인할 것**
- 계산한 벗어난 거리 vs 차량이 기록한 `cte_m`: 최대 차이 1e-7 m — 같은 값을 재현한다
- 창 탐색 최단거리 = 전수 탐색 결과와 완전 일치 (차이 0)
- GPS 정밀도(실측 4개 파일):
  - `record.csv` — 근거 `fix_cov_xx`, 고정급 686 / 낮음 120점, σ 1.0~264 cm, `fix_status=2`
  - `rec_a`·`rec_b` — 근거 `gps_sigma_m`, 전 구간 중간 등급, σ 5.1~14.6 cm,
    `gps_quality=2 · fix_status=1` (이 두 주행에는 RTK 고정급 구간이 없다)
  - `mapping.csv` — 상태·정밀도 열이 아예 없어 판정 불가
- 낮은 등급 구간 평균 오차 0.25 m vs 고정급 0.18 m — 위치가 튀진 않았고 수신기가 정밀도를
  낮춰 보고한 것에 가깝다
- **미확인**: `gps_quality=2`가 NMEA GGA의 DGPS인지, gps.py `_classify()`의 내부 코드인지
  확인 안 됨. σ 5 cm는 DGPS치고 지나치게 좋다. 코드표를 확인해 등급 이름을 바로잡을 것

## 옛 이력
원래 공공화장실 PWA(화장실SOS)였다. 앱 코드는 제거했고 정식 산출물 문서만 `docs/`와
루트의 `화장실맵_버전1_개발계획.md` · `진행상황_인수인계.md`에 남아 있다. 지금 앱과는
무관하니 참고하지 말 것.
